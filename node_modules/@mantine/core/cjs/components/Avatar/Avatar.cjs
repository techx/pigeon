'use client';
'use strict';

var React = require('react');
var getSize = require('../../core/utils/get-size/get-size.cjs');
var createVarsResolver = require('../../core/styles-api/create-vars-resolver/create-vars-resolver.cjs');
require('clsx');
require('@mantine/hooks');
require('../../core/MantineProvider/Mantine.context.cjs');
require('../../core/MantineProvider/default-theme.cjs');
require('../../core/MantineProvider/MantineProvider.cjs');
require('../../core/MantineProvider/MantineThemeProvider/MantineThemeProvider.cjs');
var useProps = require('../../core/MantineProvider/use-props/use-props.cjs');
var useStyles = require('../../core/styles-api/use-styles/use-styles.cjs');
var Box = require('../../core/Box/Box.cjs');
var polymorphicFactory = require('../../core/factory/polymorphic-factory.cjs');
require('../../core/DirectionProvider/DirectionProvider.cjs');
var AvatarGroup = require('./AvatarGroup/AvatarGroup.cjs');
var AvatarGroup_context = require('./AvatarGroup/AvatarGroup.context.cjs');
var AvatarPlaceholderIcon = require('./AvatarPlaceholderIcon.cjs');
var Avatar_module = require('./Avatar.module.css.cjs');

const defaultProps = {};
const varsResolver = createVarsResolver.createVarsResolver(
  (theme, { size, radius, variant, gradient, color, autoContrast }) => {
    const colors = theme.variantColorResolver({
      color: color || "gray",
      theme,
      gradient,
      variant: variant || "light",
      autoContrast
    });
    return {
      root: {
        "--avatar-size": getSize.getSize(size, "avatar-size"),
        "--avatar-radius": radius === void 0 ? void 0 : getSize.getRadius(radius),
        "--avatar-bg": color || variant ? colors.background : void 0,
        "--avatar-color": color || variant ? colors.color : void 0,
        "--avatar-bd": color || variant ? colors.border : void 0
      }
    };
  }
);
const Avatar = polymorphicFactory.polymorphicFactory((_props, ref) => {
  const props = useProps.useProps("Avatar", defaultProps, _props);
  const {
    classNames,
    className,
    style,
    styles,
    unstyled,
    vars,
    src,
    alt,
    radius,
    color,
    gradient,
    imageProps,
    children,
    autoContrast,
    ...others
  } = props;
  const ctx = AvatarGroup_context.useAvatarGroupContext();
  const [error, setError] = React.useState(!src);
  const getStyles = useStyles.useStyles({
    name: "Avatar",
    props,
    classes: Avatar_module,
    className,
    style,
    classNames,
    styles,
    unstyled,
    vars,
    varsResolver
  });
  React.useEffect(() => setError(!src), [src]);
  return /* @__PURE__ */ React.createElement(Box.Box, { ...getStyles("root"), mod: { "within-group": ctx.withinGroup }, ref, ...others }, error ? /* @__PURE__ */ React.createElement("span", { ...getStyles("placeholder"), title: alt }, children || /* @__PURE__ */ React.createElement(AvatarPlaceholderIcon.AvatarPlaceholderIcon, null)) : /* @__PURE__ */ React.createElement(
    "img",
    {
      ...imageProps,
      ...getStyles("image"),
      src,
      alt,
      onError: (event) => {
        setError(true);
        imageProps?.onError?.(event);
      }
    }
  ));
});
Avatar.classes = Avatar_module;
Avatar.displayName = "@mantine/core/Avatar";
Avatar.Group = AvatarGroup.AvatarGroup;

exports.Avatar = Avatar;
//# sourceMappingURL=Avatar.cjs.map
